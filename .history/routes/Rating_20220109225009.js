const express = require("express");
// Rating model
const RatingModel = require("../models/Rating");
const router = new express.Router();

const rateController = require("../controllers/")
router.post("/giveRate")
module.exports = router;
